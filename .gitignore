# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
# Capacitor web bundle (npm run build:mobile) — regenerated fresh by
# Xcode Cloud (ios/App/ci_scripts/ci_post_clone.sh) and by anyone doing a
# local native build; nothing in this repo reads the committed copy.
dist-mobile
.output
.vinxi
.tanstack/**
.nitro
.vercel
*.local
.env
exports/

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor / local AI-tooling directories — machine-specific, not needed to
# build or run the app; each dev keeps their own local copy.
.vscode/*
!.vscode/extensions.json
.idea
.claude/
.cursor/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
