/**
 * Launch gate: non-core / legacy screens stay in the codebase but are
 * hidden from the product. Flip to `true` to re-enable without deleting routes.
 */
export const LEGACY_SCREENS_ENABLED = false;
